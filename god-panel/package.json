{
  "name": "@zenobank/god-panel",
  "version": "1.0.0",
  "private": true,
  "description": "ZenōBank — God Panel (orquestração e monitoramento central)",
  "scripts": {
    "build": "echo 'build: god-panel — stack not defined yet'",
    "test": "echo 'test: god-panel — stack not defined yet'",
    "lint": "echo 'lint: god-panel — stack not defined yet'",
    "start": "echo 'start: god-panel — stack not defined yet'",
    "start:dev": "echo 'start:dev: god-panel — stack not defined yet'"
  }
}
